# Recipe Images

Images are optional.

Priority:
1. Open/reusable real image, with source/license/attribution recorded.
2. AI-generated representative image when useful; the image itself must include a small corner label saying `AI-generated image`.
3. No image.

`image-manifest.json` is the central record used when recipe images are added.
